import React from 'react';

const Paginator = () => (
    <span className="pagination-controls">
        Would probably try to do some infinite scrolling of users
    </span>
);

export default Paginator;
